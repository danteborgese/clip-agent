# Supabase (required for inbox + workers)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# GitHub workflow dispatch (optional; if set, submitting a job auto-triggers the Action)
GITHUB_TOKEN=
GITHUB_OWNER=
GITHUB_REPO=
